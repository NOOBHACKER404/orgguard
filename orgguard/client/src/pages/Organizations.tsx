import {useEffect,useMemo,useState} from 'react'; import {Search,ArrowLeft} from 'lucide-react'; import {Link} from '@/components/HashLink'; import {loadData} from '@/data/data'; import type {Organization} from '@/data/types'; import OrganizationCard from '@/components/OrganizationCard';
export default function Organizations(){const [data,setData]=useState<Organization[]>([]);const [q,setQ]=useState('');useEffect(()=>{loadData().then(x=>setData(x.organizations))},[]);const list=useMemo(()=>data.filter(o=>`${o.name} ${o.id} ${o.sector} ${o.city}`.toLowerCase().includes(q.toLowerCase())),[data,q]);return <div className="data-page"><div className="container"><Link className="back-link" href="#/"><ArrowLeft size={15}/> Back to GUARDIAN</Link><div className="page-heading"><div><div className="section-kicker">Organization intelligence</div><h1>Know who you <span>partner with.</span></h1><p>Inspect organization status, risk signals and review dates.</p></div><div className="record-count"><strong>{list.length}</strong><span>organizations</span></div></div><div className="toolbar"><div className="search-box"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search organization, sector, city..."/></div></div><div className="profile-grid page-grid">{list.map(o=><OrganizationCard key={o.id} o={o}/>)}</div></div></div>}
