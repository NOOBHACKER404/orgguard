import {Menu, Search, ShieldCheck, X, ArrowRight} from 'lucide-react';
import {Link, useLocation} from 'wouter';
import {useState} from 'react';
export default function SiteHeader(){const [open,setOpen]=useState(false);const [,go]=useLocation();const links=[['About','/about'],['Verification','/verification'],['Profiles','/profiles'],['Organizations','/organizations'],['Contact','/contact']];return <><header className="site-header"><Link className="brand" href="#/"><span className="brand-mark"><ShieldCheck size={19}/></span><span>GUARDIAN<small>PBVS / ORGGUARD</small></span></Link><nav className="desktop-nav">{links.map(([t,h])=><Link key={h} href={h}>{t}</Link>)}</nav><div className="header-actions"><button className="icon-button" onClick={()=>go('/search')} aria-label="Search"><Search size={18}/></button><Link className="outline-button desktop-only" href="#/verification">Verify a record <ArrowRight size={15}/></Link><button className="menu-button" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>{open&&<div className="mobile-menu"><div className="mobile-menu-inner">{links.map(([t,h])=><Link key={h} href={h} onClick={()=>setOpen(false)}>{t}<span>›</span></Link>)}<Link className="mobile-verify" href="#/verification" onClick={()=>setOpen(false)}>Verify a record <ArrowRight size={16}/></Link></div></div>}</>}
