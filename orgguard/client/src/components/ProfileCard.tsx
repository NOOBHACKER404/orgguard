import {ArrowRight, FileCheck2} from 'lucide-react'; import {Link} from 'wouter'; import type {Profile} from '@/data/types';
const colors:any={BLACK:'#111827',RED:'#ef4444',YELLOW:'#eab308',GREEN:'#16a34a'};
export default function ProfileCard({p}:{p:Profile}){return <Link href={`/profiles/${p.file}`} className="profile-card"><span className="file-no">{p.file}</span><span className="status-chip" style={{background:`${colors[p.status]}18`,color:colors[p.status]}}>{p.status}</span><div className="profile-main"><span className="avatar" style={{background:colors[p.status]}}>{p.name.split(' ').slice(-2).map(x=>x[0]).join('').slice(0,2)}</span><div><h3>{p.name}</h3><p>{p.role}</p></div></div><div className="profile-note"><FileCheck2 size={12}/>{p.note}</div><div className="check-more">View verification record <ArrowRight size={14}/></div></Link>}
